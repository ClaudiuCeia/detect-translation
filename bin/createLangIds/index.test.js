// For tracking overall tests

// should detect when page is translated (on load or at any time after load)
// should detect translator and target language
// should create standard lang tag for translated content
// should correct Adobe `language` prop or fire `newPage` event with new lang
// should correct the HTML element’s lang attribute (for accessibility)
// should then return `true` to `isPageTranslated`
